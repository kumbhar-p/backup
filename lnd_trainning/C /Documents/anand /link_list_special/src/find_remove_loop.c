#include "header.h"

struct node *find_remove_loop (struct node *head)
{
		if (head == NULL) {

				printf ("List is empty\n");

				return head;

		}

		struct node *slow = NULL;

		struct node *fast = NULL;
}
